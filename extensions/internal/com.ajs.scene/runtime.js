const script = {
    // Scene extension - runtime methods
    // Scenes are containers that hold game objects
    // Runtime scene management is handled by the game engine

    onCreated(_object) {
        // Called when scene is loaded at runtime
    },

    onUpdated(_object) {
        // Called when scene is updated at runtime
        // Scene updates are managed by the game engine
    },

    onDestroyed(_object) {
        // Called when scene is destroyed/unloaded at runtime
    }
};
